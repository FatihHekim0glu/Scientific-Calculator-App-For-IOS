import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Scientific Calculator App For IOS")
                .font(.largeTitle)
            Text("Scientific Calculator")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
